<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--Inherited Template for purchase order report-->
    <record id="report_purchaseorder_document_customized" model="ir.ui.view">
        <field name="name">report_purchaseorder_document_customized</field>
        <field name="inherit_id" ref="purchase.report_purchaseorder_document"/>
        <field name="priority">10</field>
        <field name="arch" type="xml">
            <xpath expr="//t[@t-call='account.document_tax_totals']" position="replace">
                <t t-call="account.document_tax_totals">
                    <t t-set="tax_totals" t-value="o.tax_totals"/>
                    <t t-set="currency" t-value="o.currency_id"/>
                    <t t-set="amt_discount" t-value="o.amount_discount"/>
                </t>
            </xpath>
        </field>
    </record>
</odoo>
